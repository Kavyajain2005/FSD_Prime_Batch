const handleTextChange = (a) => {
    console.log(a);
    console.log(
        "Change: ",
        a.target.value
    );
}

const handleKeyDown = (a) => {
    console.log(a);
    console.log(
        "Key Down: ",
        a.target.value
    );
}

const handleKeyUp = (a) => {
    console.log(a);
    console.log(
        "Key Up: ",
        a.target.value
    );
}